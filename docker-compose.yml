services:
  app:
    build: .
    ports:
      - "3000:3000"
    env_file:
      - .env
    volumes:
      - db-data:/app/prisma
      - uploads-data:/app/uploads
    restart: unless-stopped

volumes:
  db-data:
  uploads-data:
